export default async function decorate(block) {
 console.log("block: ", block);

 // 1. grab the href value of <a> link
 // 2. Fetch the JSON data from the link
 // 3. Using the data object, create the necessary HTML elements and populate them with the data

 
}
